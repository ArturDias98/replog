services:
  replog-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "4200:80"
    env_file:
      - .env
    environment:
      - API_BASE_URL=${API_BASE_URL}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
